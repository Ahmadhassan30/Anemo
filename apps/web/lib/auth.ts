/*
 * Purpose: NextAuth configuration placeholder.
 */
import type { NextAuthOptions } from "next-auth";

export const authOptions: NextAuthOptions = {
  // TODO: add providers, callbacks, and session config
};
